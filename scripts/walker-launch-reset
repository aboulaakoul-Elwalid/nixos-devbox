#!/usr/bin/env bash
set -euo pipefail
W="$HOME/.nix-profile/bin/walker"
[ -x "$W" ] || W="/run/current-system/sw/bin/walker"
# Reset any stuck launcher UI, keep service if running
"$W" -q >/dev/null 2>&1 || true
sleep 0.08
exec "$W"
