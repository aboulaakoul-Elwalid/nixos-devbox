general {
    lock_cmd = omarchy-lock-screen                         # lock screen and 1password
    before_sleep_cmd = loginctl lock-session               # lock before suspend.
    after_sleep_cmd = /home/elwalid/.local/bin/hyprshell-display-resume # restore display and Hyprshell after suspend.
    inhibit_sleep = 3                                      # wait until screen is locked
}

listener {
    timeout = 3600                                            # 1h
    on-timeout = pidof hyprlock || omarchy-launch-screensaver # start screensaver (if we haven't locked already)
}

listener {
    timeout = 7200                     # 2h
    on-timeout = loginctl lock-session # lock screen when timeout has passed
}

listener {
    timeout = 7230                                           # 2h + 30s
    on-timeout = brightnessctl -sd '*::kbd_backlight' set 0  # save state and turn off keyboard backlight
    on-resume = brightnessctl -rd '*::kbd_backlight'         # restore keyboard backlight
}

listener {
    timeout = 7230                                           # 2h + 30s
    on-timeout = hyprctl dispatch dpms off                   # screen off when timeout has passed
    on-resume = /home/elwalid/.local/bin/hyprshell-display-resume # restore display and Hyprshell after DPMS
}
