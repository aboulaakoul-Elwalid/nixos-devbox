# Nix Flake Project
# Copy to your project as .envrc, then run: direnv allow

# Load nix development environment from flake.nix
use nix

# Load secrets from .env.local (gitignored)
dotenv_local

# Add local scripts to PATH
use_local_bin
