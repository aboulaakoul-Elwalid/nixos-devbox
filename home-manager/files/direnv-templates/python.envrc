# Simple Python Project
# Copy to your project as .envrc, then run: direnv allow

# Create/activate Python venv
layout python python3

# Load .env.local for secrets (gitignored)
dotenv_local

# Add local scripts to PATH
use_local_bin
